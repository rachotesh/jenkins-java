pipeline {

    agent {
        docker {
            image 'openjdk:latest'
        }
    }

    stages {

        stage('Compile') {
            steps {
                sh 'javac HelloWorld.java'
            }
        }

        stage('Run') {
            steps {
                sh 'java HelloWorld'
            }
        }
    }
}
