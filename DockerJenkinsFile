pipeline {

    agent {
        docker {
            image 'eclipse-temurin:17'
        }
    }

    stages {

        stage('Compile') {
            steps {
                sh 'javac HelloWorld.java'
            }
        }

        stage('Run') {
            steps {
                sh 'java HelloWorld'
            }
        }
    }
}
